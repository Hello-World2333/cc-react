/**
 * Demo data — the feature list shown on the Home tab. A plain .ts module (no
 * JSX): imported arrays / strings are bundled into the same Lua module and
 * work like any other value (see also lib/format.ts for pure functions).
 */

export const FEATURES = [
  'useState / useEffect hooks',
  'flexbox layout + colors',
  'Box Panel Text Button Input',
  'Scroll: wheel + drag',
  'keyboard input + focus',
  'async/await + fetch',
  'useRequest 3-state',
  'multi-file import',
];
